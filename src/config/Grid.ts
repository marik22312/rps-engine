export const GRID_HEIGHT: number = ((process.env.GRID_HEIGHT as unknown) as number) || 7;
export const GRID_WIDTH: number = ((process.env.GRID_WIDTH as unknown) as number) || 6;
export const PLAYER_ROWS: number = ((process.env.GRID_WIDTH as unknown) as number) || 2;
export const LETTERS: string = 'ABCDEFGHIJ';
